# Probes

Diagnostic scripts for inspecting a live sandbox group.

* `sandbox_introspect.py` — dump kernel, network interfaces, mounted volumes,
  available disk images
* `sandbox_egress_audit.py` — verify what the configured egress policy
  actually allows / blocks

Run with the same `.env` as the examples.
